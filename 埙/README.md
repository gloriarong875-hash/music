# 金声玉振 · 埙制作互动

当前实现埙制作流程的第一步“揉捏排气”。

## 运行

这是一个无依赖的静态网页，可直接打开 `index.html`，或在项目目录启动任意静态文件服务器。

```powershell
python -m http.server 4173
```

浏览器访问 `http://localhost:4173`。

## 交互

- 按住键盘左方向键或右方向键揉泥。
- 也可按住页面两侧的方向按钮。
- 持续揉捏约 3 秒完成排气。
- 点击“重新体验”可重置第一步。
